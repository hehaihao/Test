<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 圆角 -->
    <corners
        android:radius="@dimen/dp30"
        android:topLeftRadius="@dimen/dp30"
        android:topRightRadius="@dimen/dp30"
        android:bottomLeftRadius="@dimen/dp30"
        android:bottomRightRadius="@dimen/dp30"/><!-- 设置圆角半径 -->
    <!-- 间隔 -->
    <padding
        android:left="@dimen/dp2"
        android:top="@dimen/dp2"
        android:right="@dimen/dp2"
        android:bottom="@dimen/dp2"/><!-- 各方向的间隔 -->

    <!-- 大小 -->
    <size
        android:width="@dimen/dp100"
        android:height="@dimen/dp30"/><!-- 宽度和高度 -->

    <solid
        android:color="@color/blue1e"/>

</shape>